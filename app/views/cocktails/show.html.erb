<h2><%= @cocktail.name %></h2>

<ul>
  
  <% @cocktail.doses.each do |dose| %>
    <li>
      <%= dose.description %> of <%= dose.ingredient.name %>

      <%= link_to "💩", dose_path(dose.id), method: :delete %>
      <%#= link_to "💩", dose_path(dose) %>
      <%#= link_to "💩", dose %>
    </li> 
  <% end %>

</ul>

<%#= link_to 'Create a new dose for this cocktail', new_cocktail_dose_path(@cocktail.id) %>
<%= link_to 'Create a new dose for this cocktail', new_cocktail_dose_path(@cocktail) %>

<%= link_to 'back', cocktails_path %>